# Saree Sanskruti Mobile App

React Native mobile application built with Expo, TypeScript, and React Navigation.

## Prerequisites

- Node.js v20.19.6
- npm 9.9.4
- Expo CLI (installed globally or via npx)

## Setup

1. Install dependencies:
```bash
cd mobile-app
npm install
```

2. Create `.env` file in the root directory:
```
EXPO_PUBLIC_API_URL=https://api.sarisanskuti.in/api
```

3. Start the development server:
```bash
npx expo start
```

## Project Structure

```
mobile-app/
├── App.tsx                 # Root component
├── package.json
├── tsconfig.json
├── app.json
└── src/
    ├── navigation/
    │   └── AppNavigator.tsx
    ├── screens/
    │   ├── HomeScreen.tsx
    │   ├── LoginScreen.tsx
    │   └── RegisterScreen.tsx
    ├── services/
    │   └── api.ts
    └── types/
        └── navigation.ts
```

## Features

- Login and Registration screens
- API integration with error handling
- Type-safe navigation
- Strict TypeScript configuration
- Production-ready structure


React Native mobile application built with Expo, TypeScript, and React Navigation.

## Prerequisites

- Node.js v20.19.6
- npm 9.9.4
- Expo CLI (installed globally or via npx)

## Setup

1. Install dependencies:
```bash
cd mobile-app
npm install
```

2. Create `.env` file in the root directory:
```
EXPO_PUBLIC_API_URL=https://api.sarisanskuti.in/api
```

3. Start the development server:
```bash
npx expo start
```

## Project Structure

```
mobile-app/
├── App.tsx                 # Root component
├── package.json
├── tsconfig.json
├── app.json
└── src/
    ├── navigation/
    │   └── AppNavigator.tsx
    ├── screens/
    │   ├── HomeScreen.tsx
    │   ├── LoginScreen.tsx
    │   └── RegisterScreen.tsx
    ├── services/
    │   └── api.ts
    └── types/
        └── navigation.ts
```

## Features

- Login and Registration screens
- API integration with error handling
- Type-safe navigation
- Strict TypeScript configuration
- Production-ready structure


